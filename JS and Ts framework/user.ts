export interface User {
  id: number;
  name: string;
  email: string;
}


// วิธีการ import ไปใช้
// import { User } from "@/type/user";

// const user: User = { id: 1, name: "KLUY", email: "a@email.com" };
